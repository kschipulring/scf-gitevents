{{yield}}

<ul id="git-users">
  {{#each users.members as |user|}}
    <li id="users_{{user.id}}" class="list-group-item {{if user.active "active"}}" {{action "chooseUser" user}}>{{user.login}}</li>
  {{/each}}
</ul>